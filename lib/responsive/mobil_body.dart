import 'package:flutter/material.dart';

class MobilBody extends StatelessWidget {
  const MobilBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("je suis un mobile"),
    );
  }
}